
'use client';
// This component will be created in a future step.
// For now, it's a placeholder.
export function RoadmapCreation({ onCancel }: { onCancel: () => void }) {
    return (
        <div>
            <h1>Create Roadmap (Placeholder)</h1>
            <button onClick={onCancel}>Cancel</button>
        </div>
    );
}
